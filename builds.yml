build_one:
  triggered_by:
    - test.stuff.*
  repo:
    owner: coldog
    project: ci-sample
    provider: github

  build:
    build:

      services:
        mysql:
          image: 'mysql:5.7'
          env:
            MYSQL_ROOT_PASSWORD: pass


      base_image: ubuntu
      env:
        TEST: stuff
        TEST_1: things
        TEST_2: things2
